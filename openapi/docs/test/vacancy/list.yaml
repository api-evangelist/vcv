get:
  description: Get vacancy tests list
  tags:
    - test
  parameters:
    - $ref: '../../components/parameters.yaml#/pagination_page'
    - $ref: '../../components/parameters.yaml#/pagination_page_size'
    - name: with[]
      in: query
      description: Relations to retrieve
      required: false
      schema:
        type: array
        items:
          type: string
          enum: [ 'questions', 'questions.answers', 'questions.media', 'questions.variables' ]
    - $ref: '../../components/parameters.yaml#/filter_id'
    - $ref: '../../components/parameters.yaml#/filter_vacancy_id'
    - $ref: '../../components/parameters.yaml#/filter_created_at_from'
    - $ref: '../../components/parameters.yaml#/filter_created_at_to'
  responses:
    '200':
      description: list of vacancy tests
      content:
        application/json:
          schema:
            properties:
              _total_items:
                type: integer
              _page:
                type: integer
              _page_count:
                type: integer
              _links:
                properties:
                  self:
                    type: object
                    properties:
                      href:
                        type: string
                        format: uri
                  next:
                    type: object
                    properties:
                      href:
                        type: string
                        format: uri
                  last:
                    type: object
                    properties:
                      href:
                        type: string
                        format: uri
                type: object
              _embedded:
                type: object
                properties:
                  vacancy_tests:
                    type: array
                    items:
                      allOf:
                        - $ref: '../../components/test.yaml#/VacancyTest'
                        - type: object
                          properties:
                            _embedded:
                              type: object
                              properties:
                                questions:
                                  type: array
                                  items:
                                    allOf:
                                      - $ref: '../../components/test.yaml#/VacancyTestQuestion'
                                      - type: object
                                        properties:
                                          _embedded:
                                            type: object
                                            properties:
                                              answers:
                                                type: array
                                                items:
                                                  $ref: '../../components/test.yaml#/VacancyTestAnswer'
                                              variables:
                                                type: array
                                                items:
                                                  $ref: '../../components/test.yaml#/VacancyTestQuestionVariable'
                                              media:
                                                $ref: '../../components/test.yaml#/VacancyTestQuestionMedia'
            type: object